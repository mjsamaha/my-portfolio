{
  "name": "functions",
  "description": "Firebase Functions for my-portfolio",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "echo 'No build step'",
    "start": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^11.11.0",
    "firebase-functions": "^4.4.0"
  }
}
