/**
 * Template for `src/environments/firebase.config.ts`.
 * Copy this to `src/environments/firebase.config.local.ts` and fill real values for local development.
 * DO NOT commit `firebase.config.local.ts`.
 */

export const firebaseConfig = {
  apiKey: 'REPLACE_ME',
  authDomain: 'REPLACE_ME',
  projectId: 'REPLACE_ME',
  storageBucket: 'REPLACE_ME',
  messagingSenderId: 'REPLACE_ME',
  appId: 'REPLACE_ME',
  measurementId: 'REPLACE_ME'
};
